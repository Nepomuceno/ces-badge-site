node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.nitro
.tanstack
.output
.vinxi
todos.json

# Runtime data generated by the app (logos.json, votes.json, etc.)
server/runtime-data/
# Local roster data contains PII; keep it out of Git history
server/data/allowed-users.json
# Generic data directory for custom DATA_DIR mounts
data/
# Logos from the user 
public/logos
!public/logos/.gitkeep
